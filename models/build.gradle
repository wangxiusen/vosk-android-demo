apply plugin: 'com.android.library'

android {
    namespace "com.huimei.voice.models"
    compileSdkVersion 36
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 36
    }
    buildFeatures {
        buildConfig = false
    }
}
